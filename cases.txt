# Temporary work. to be deleted.
#INCLUDE cases_msc_temp.txt

# Put new test case definitions here unless they should be organized in a
# separate file, such as when it's a big batch imported from somewhere.
INCLUDE cases_new.txt

# Original benchmark decks
INCLUDE cases_benchmarks.txt

# Test cases ported from Mecway. The critera are designed to test for specific
# behaviors.
INCLUDE cases_vic.txt

# Test cases ported from Mecway but using bulk comparison instead of individual
# values. Becuase it's using generic test critera, it's more likely to get
# false fails which you can correct by increasing the tolerances.
# If these cases cause difficulties, they can all be removed because
# cases_vic.txt tests the same decks in a more intentional way.
INCLUDE cases_vic_bulk.txt
